---
title: AutoDL基础知识
date: 2025-05-23
---

1. AutoDL 是一个算力租用平台，可以按量、按天租用 GPU 卡或整租一台服务器，适合完成快速简单的测试。
2. 使用 AutoDL 可以快速部署模型，通过租用算力实例并配置镜像，可以在算力实例上部署模型。
3. ollama 是一个用于部署模型的工具，可以通过在算力实例上安装 ollama 并执行相应命令来下载并运行模型。
4. AutoDL 提供了一种 SSH 隧道的方式，可以将实例中的端口映射到公网可供访问的地址，使外部用户也能访问模型。
5.
